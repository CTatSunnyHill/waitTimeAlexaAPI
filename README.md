This repository contains code to create a Puppeteer program that launches a headless browser to search in hospital wait times from a specified website. The headless browser searches for hospital wait times for ER in a specified vicinity as set by the user and then scrapes those hospital names and their corresponding wait times into a JSON format.
There is also code to create an AWS Lambda function that has been linked with an Alexa Skill that is waiting for publishing.
